---
import Layout from '../layouts/Layout.astro';
import EducationSection from '../components/EducationSection.astro';
import { education, site } from '../data/site';
---

<Layout
	title={`About — ${site.name}`}
	description={`Technical background, focus areas, and what ${site.name} is currently seeking.`}
>
	<header class="space-y-3">
		<h1 class="text-3xl font-semibold tracking-tight text-[rgb(var(--fg))]">About</h1>
		<p class="max-w-2xl text-base leading-7 text-[rgb(var(--muted))]">
			I like shipping products that feel simple on the surface and are thoughtfully engineered underneath.
		</p>
	</header>

	<section class="mt-8 grid gap-6 sm:grid-cols-2">
		<div class="grid-panel p-6">
			<h2 class="mono text-sm font-semibold uppercase tracking-wider text-[rgb(var(--fg))]">
				Background
			</h2>
			<ul class="mt-4 list-disc space-y-2 pl-4 text-sm leading-6 text-[rgb(var(--muted))]">
				<li>Full-stack engineer focused on product velocity without sacrificing quality.</li>
				<li>Prefer small, composable systems with clear boundaries and failure modes.</li>
				<li>Care about accessibility, performance, and maintainability from day one.</li>
			</ul>
		</div>

		<div class="grid-panel p-6">
			<h2 class="mono text-sm font-semibold uppercase tracking-wider text-[rgb(var(--fg))]">
				Focus areas
			</h2>
			<ul class="mt-4 list-disc space-y-2 pl-4 text-sm leading-6 text-[rgb(var(--muted))]">
				<li>Frontend systems: design tokens, component architecture, and UX clarity.</li>
				<li>Data ingestion: validation, normalization, and observability.</li>
				<li>Performance: minimal JS, fast navigations, and predictable rendering.</li>
			</ul>
		</div>

		<div class="sm:col-span-2">
			<EducationSection education={education} />
		</div>

		<div class="grid-panel p-6 sm:col-span-2">
			<h2 class="mono text-sm font-semibold uppercase tracking-wider text-[rgb(var(--fg))]">
				What I’m seeking
			</h2>
			<div class="mt-4 grid gap-4 sm:grid-cols-2">
				<div class="space-y-2">
					<p class="text-sm text-[rgb(var(--muted))]">
						I’m excited about roles where I can own meaningful product surface area and ship.
					</p>
					<ul class="list-disc space-y-2 pl-4 text-sm leading-6 text-[rgb(var(--muted))]">
						<li>Full-time or contract, product-focused teams</li>
						<li>High bar for UX and engineering craft</li>
						<li>Clear priorities, quick feedback loops</li>
					</ul>
				</div>
				<div class="space-y-2">
					<p class="text-sm text-[rgb(var(--muted))]">Best way to reach me:</p>
					<a
						class="focus-ring inline-flex w-fit items-center justify-center bg-[rgb(var(--accent))] px-4 py-2 text-sm font-semibold text-white no-underline shadow-sm hover:opacity-95"
						href={`mailto:${site.email}`}
					>
						Email {site.email}
					</a>
				</div>
			</div>
		</div>
	</section>
</Layout>


